[loggers]
keys=root

[logger_root]
handlers=file
level=NOTSET

[formatters]
keys=simple

[formatter_simple]
format=%(asctime)s %(levelname)s %(funcName)s(%(lineno)d) %(message)s

[handlers]
keys=file

[handler_file]
class=FileHandler
formatter=simple
level=NOTSET
args=('logs/server.log',)